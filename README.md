# SquamSandbox Project Scheduler

This repository contains a simple full-stack web application that lets you:

- Create projects and tasks
- Assign tasks to time blocks on a timeline
- Drag and drop tasks to reschedule them

## Running

```bash
npm install
node server.js
```

Then open `http://localhost:3000` in your browser.
